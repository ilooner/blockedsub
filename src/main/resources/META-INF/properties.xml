<?xml version="1.0"?>
<configuration>
  <!--
  <property>
    <name>dt.application.{appName}.operator.{opName}.prop.{propName}</name>
    <value>some-default-value (if value is not specified, it is required from the user or custom config when launching)</value>
  </property>
  -->
  <!-- memory assigned to app master
  <property>
    <name>dt.attr.MASTER_MEMORY_MB</name>
    <value>1024</value>
  </property>
  -->
  <property>
    <name>dt.operator.*.attr.TIMEOUT_WINDOW_COUNT</name>
    <value>2000000</value>
  </property>

  <property>
    <name>dt.operator.*.attr.HEARTBEAT_TIMEOUT_MILLIS</name>
    <value>2000000000</value>
  </property>
  <property>
    <name>dt.loggers.level</name>
    <value>com.datatorrent.bufferserver.*:DEBUG,com.datatorrent.netlet.*:DEBUG</value>
  </property>
</configuration>

